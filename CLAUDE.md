# CLAUDE.md

用于约束编码行为，目标是减少无关改动、过度设计和不必要的上下文消耗。

## 0. 语言与沟通

**默认使用中文回复，同时保持技术表达准确。**

- 除非用户明确要求其他语言，否则解释、计划、进度更新和最终回复使用中文。
- 代码、命令、文件名、路径、API 名称、错误信息和正式技术术语保持原文。
- 不要为了中文化而翻译标识符、协议字段、配置键或第三方产品正式名称。

## 1. 编码前先判断

**不要擅自假设会影响结果的重要事实。**

- 对会影响数据、安全、架构、权限或用户可见行为的重要歧义，先询问。
- 对低风险、容易撤销的小细节，采用与现有代码最一致的合理解释继续执行，并在结果中说明。
- 存在明显更简单的方案时，优先采用简单方案。
- 不要为了消除所有不确定性而扩大仓库调查范围。
- 项目范围、目录边界、数据真实性、安全和验证要求以各级 `AGENTS.md` 为准。

## 2. 简单优先

**只编写解决当前问题所需的最少代码，不做投机性设计。**

- 不添加用户没有要求的功能。
- 不为一次性代码创建不必要的抽象层。
- 不增加未被要求的“灵活性”或“可配置性”。
- 不为极不可能发生的情况堆叠复杂错误处理。
- 如果明显存在更短、更清晰、同样可靠的实现，优先简化。

## 3. 精准修改

**只修改完成任务所必需的内容。**

- 不顺手改进无关代码、注释、格式或命名。
- 不重构没有损坏且与任务无关的代码。
- 遵循项目现有风格。
- 发现无关的废弃代码或风险时可以指出，但不要自行清理。
- 删除仅因本次改动而变得无用的导入、变量或函数。
- 每一处改动都应能直接追溯到用户请求。

## 4. 以目标驱动执行

**先定义可验证的成功标准，再完成修改。**

- 涉及多个文件、多个系统或明显需要多步骤执行的任务，先给出简短计划。
- 对局部、低风险、明确的小修改，直接执行，不需要形式化计划。
- 修复缺陷时，优先使用能够复现问题的聚焦测试或最小验证。
- 对适合自动化测试覆盖的行为变化，优先增加或更新聚焦测试。
- 对纯样式、文案或不适合自动化的小修改，使用最小针对性验证即可，不强制新增测试。
- 不要为了“更完整”而运行与本次修改无关的全仓库检查。

## 5. 上下文压缩与交接

**长会话或阶段性任务完成后，主动压缩上下文，避免持续携带无效历史。**

当会话较长、上下文明显膨胀，或一个独立阶段已经完成时：

- 将关键信息更新到 `progress.md`。
- 只保留：当前目标、已完成、未完成、关键文件和路径、重要技术决定、已确认限制或风险、下一步。
- 删除：重复讨论、已解决报错细节、无效尝试、长日志和大段代码原文。
- `progress.md` 应保持简洁，单次交接摘要优先控制在约 500–800 字以内。
- 完成一个独立阶段后，优先新开会话。
- 新会话开始时先阅读：根目录 `AGENTS.md`、相关子目录 `AGENTS.md`、`CLAUDE.md` 和 `progress.md`，然后只处理当前任务。
- 不要为了恢复上下文重新扫描整个仓库，除非当前任务确实需要。

## 6. 完成标准

任务完成时应满足：

- 改动范围与用户请求一致。
- 没有无关重构或投机性功能。
- 已执行与改动相称的验证。
- 明确报告已验证和未验证的部分。
- 如果仍有重要风险或假设，简洁说明。
