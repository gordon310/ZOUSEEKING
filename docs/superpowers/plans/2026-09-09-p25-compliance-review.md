# P2-5 发布与合规 · 勘察清单(2026-09-09 凌晨)

## 现状盘点

| 域 | 已有 | 缺口 |
|---|---|---|
| PWA 打包 | 无 manifest / service worker / 图标集 | **首缺口**:D3 PWA 先行 → 需 manifest.webmanifest + sw(缓存策略)+ 图标(PWA 可安装);桌面/390 移动适配样式已有 |
| 隐私/条款页面 | `web/privacy.html` / `web/terms.html`(基础) | 版本与 docs/legal 对齐检查(privacy-2026-08);上线前运营主体替换(support@…example 占位);收集用途清单核对 |
| 合规文档 | docs/legal 全套:privacy-policy / terms / DSAR 流程 / 事故响应 / 运营 runbook | 上线清单化(见下) |
| 后端隐私 | /api/privacy + 账户删除请求(202)+ 删除执行器 | staging 验证删除流;consent 版本记录核对 |
| 渠道打包 | 无商店工程(安卓/iOS) | D3 决定 PWA 先行验证后商店跟进 → 本轮不需要原生包,预留:打包壳评估(后续) |

## 合规清单草案(上线前逐项核)

1. **ICP 备案/合规**:域名+服务器(大陆访问)ICP 备案状态;Render staging 为海外 —— 上线主域名若大陆可访问需备案(或 CDN/合规路径决策,运营主体动作)。
2. **个人信息保护(个保法)**:处理范围=账户邮箱/照片(六类现场照含私密空间?)/定位/房产地址 → 目的+最小化+删除通道(docs/legal DSAR 已备)已满足设计;**照片类目属敏感(个人住宅内部)** → 单独同意 + 本地处理优先说明核对。
3. **AI/深度合成评估**:JPPGSKILL analyze(LLM 图像分析)与报告生成是否触发《深度合成规定》/生成式 AI 备案:分析类(非面向公众生成内容)通常不触发,但报告/配图为对外输出内容 → **需法务评估结论**;后台"生成内容"如涉合成图片需标注(AGENTS 已要求 data_class 标注——展示侧"AI 生成"标识核对)。
4. **支付合规**:Stripe 直连需商户主体(大陆渠道:微信支付主体资质);D1 海外 Stripe + 大陆网页/微信 → 商户号/主体决策(运营动作,阻塞 P2-4 沙箱转正)。
5. **App 备案**(商店发布时才需;PWA 先行不触发)。
6. **出所明記**(数据合规,产品层已有):国交省数据标注 ✓(P2-3 已做);SUUMO 租金未授权不输出 ✓(D6a)。

## 建议

- P2-5 开发侧(09-15 主工期):PWA manifest/sw/图标 + privacy/terms 版本对齐 + 照片敏感同意文案核对 → 可独立小单元 P2-5a。
- 运营/法务侧(用户/主体动作,不阻塞开发):ICP 决策、微信支付主体、AI 备案评估、support 域名替换 → 列上线前置清单跟踪。
