# 10-07 上线倒排 · 每日闭环(D-13 → D-0)

> 立于 **2026-09-24 夜**(生产上线批次已执行、每日备份定时器已装)。**2026-09-24 用户拍板 4 项决策,已并入下表。**
> 规则:**每天一个可验收的闭环交付**;早班 07:30 / 夜班 20:30 自主班各认领**一个未完成的有界单元**。
> 边界不变:自主班 **commit+push 止**;DB 写 / 部署 / 凭据 / 删除 / 法务一律先出清单待批。红线:字段冻结、只 forward migration、CI 是唯一权威门。

## 决策记录(2026-09-24 用户拍板)
| # | 决策 | 对本计划的影响 |
|---|---|---|
| 1 | **开放注册,不做邀请**(不切 `disable_signup`) | 取消 D-6「注册门切换」;**新增工程改造**:注册去掉邀请码强制(现 `POST /invite-register` 强制 `invite_code`)+ AGENTS「invitation-only」条款改写 + 测试 → 并入 **D-12**;D-11 改测开放注册 |
| 2 | **授权 C13**(真实 staging smoke) | D-9 解锁;staging 凭据(`SMOKE_*`)由 Hermes 筹备,缺件即报 |
| 3 | **C04 成本上限批准** | D-8 解锁;按「最小必要」执行:不新增长期付费项,以隔离恢复演练为主 |
| 4 | **法务地区定位日本**(房产全部在日本) | D-5 法务范围 = 日本:隐私政策按日本法(個人情報保護法/特定商取引法)口径,允许地区 = 日本,不做多地区适配 |

## 固定作业(已在跑,不占当日闭环)
- 生产巡检 每日 09:30 · CI/生产心跳每 6h · 支付对账 **D-11(09-26 20:00)** · 终检 Go/No-Go **D-2(10-05 20:00)**。

## 倒排表

| D | 日期 | 今日闭环(验收口径) | 负责 | 需你/依赖 |
|---|---|---|---|---|
| **D-13** | 09-24 四 | ✅ **生产上线批次 + 每日备份定时器 + 记账入仓**(已完成) | Hermes | — |
| **D-12** | 09-25 五 | ① **开放注册改造**:新增无邀请码注册路径(复用既有校验/限流/consent/Admin 建号),注册页去邀请码必填,AGENTS 条款改写,测试更新;② **回滚演练**:真回滚→关键路径验证→前滚,产出 `docs/operations/rollback-drill-2026-10.md`;③ 安装 `observability-check.timer` | 自主班(Codex) | — |
| **D-11** | 09-26 六 | ① 支付对账(live 无 `pending/failed` 遗留单、webhook 全 `processed`、价格 ID 一致);② ADR-0002 文字改「C 端 + 后台」并复核 release-scope 三者一致;③ **开放注册端到端实测**(无邀请码直接注册成功、异常入参被拒、残留 0) | Hermes + 自主班 | — |
| **D-10** | 09-27 日 | 首周观察看板(数据/支付/任务/错误)成型 + 上线公告与试运行说明稿(四语言) | Hermes | — |
| **D-9** | 09-28 一 | **C13 真实 staging smoke**(已授权):`--execute` 产出实测证据包(证据文件由 `NOT_EXECUTED` 变实测)+ 清理残留 0 | 自主班(Codex) | 凭据由 Hermes 筹备,缺件即报 |
| **D-8** | 09-29 二 | **C04**(成本已批):provider 物理备份/PITR + 私有 Storage 隔离恢复演练(checksum、RPO/RTO、rollback 路径) | 自主班(Codex) | 停机窗由 Hermes 定 |
| **D-7** | 09-30 三 | **C05** 生产四身份(Storage/Auth/RLS)复验 + G4 终检(迁移/RLS/额度/日志) | 自主班(Codex) | — |
| **D-6** | 10-01 四 | **开放注册上线复核**:限流/账号枚举安全/邮件确认行为 + 试运行标识与开放注册口径一致(四语言) | 自主班(Codex) | — |
| **D-5** | 10-02 五 | 法务(日本口径)定稿:隐私政策/条款/免责 + 删除 SLA/事故责任人 + 商店提审材料包 | 用户 + Hermes 起草 | 法务口径确认 |
| **D-4** | 10-03 六 | 备份异地化:对象存储接入 + 备份新鲜度观测绿(脱离本地保留) | Hermes | **桶名 + 密钥** |
| **D-3** | 10-04 日 | **全链路演练**:注册→登录→生成报告→单次支付→退款/客服,端到端一次成功 | Hermes + 自主班 | — |
| **D-2** | 10-05 一 | 终检 **Go/No-Go**(全绿,或明确列出剩余阻断) | Hermes | — |
| **D-1** | 10-06 二 | 冻结:代码冻结 + 公告发布 + 看板上线 + 回滚预案待命 | Hermes | — |
| **D-0** | 10-07 三 | **C 端 + 后台 同时正式上线**;首小时 & 24h 观察窗 | 用户 + Hermes | — |

## 仍需你出手(已由 5 项减至 2 项)
1. **法务口径确认**(地区 = 日本已定;仍需:删除 SLA 天数 / 事故责任人 / 是否上特商法记载页)→ 决定 D-5
2. **对象存储桶名 + 密钥**(备份异地化)→ 决定 D-4

## 若出现滑期
只允许两档:① 延后 48 小时(10-09);② 缩小范围上线。决定权在用户,最晚 **D-1(10-06)** 拍板。
